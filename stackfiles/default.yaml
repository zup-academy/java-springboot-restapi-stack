type: app
description: Default stackfile usecase description
# template used to create the project
template: java-springboot-restapi-stack/<template-name>
inputs:
  some_input: value
# list of plugins to be applied after project creation
plugins:
  - plugin: java-springboot-restapi-stack/<plugin-name>
    inputs:
      some_input: value
      some_list:
        - item1
        - item2
